# Exercice 4:  polynôme du second degré
"""
a x² + b x + c =0
delta = b²-4ac
delta<0 => pas de soll
delta=0 => x1 = -b/2a
delta>0 => x1=(-b+sqr(delta))/2a x2=(-b-sqr(delta))/2a
"""

def polynome2(a,b,c=0):
    delta = b**2 - 4 * a * c
    if delta = 0:
        return return 

print(polynome2(2, 4))
